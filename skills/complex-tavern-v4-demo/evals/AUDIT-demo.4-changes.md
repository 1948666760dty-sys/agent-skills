# Complex Tavern v4 Demo — demo.4 change audit

## User-locked interaction profile
20 decisions were accepted exactly as provided (A A B C A / C A A A C / A C A C A / A A A A C).

## Conflicts fixed
1. Current Demo schema mismatch across clone init/state contract/run summary.
2. Stable trigger language mixed into Demo trigger/resume rules.
3. No-Rush and Demo version banner both claimed first visible position.
4. v4 Safe Pipeline and inherited v3 1–19 list looked like two executable macro pipelines.
5. Branch write scope could incorrectly reject legal ancestor memory reads.
6. Optional Trigger eligibility was not clearly separated from deterministic deadlines/appointments.
7. Context Inspector could overclaim telemetry without a real trace.
8. Date-header de-duplication lacked explicit render state.
9. Director Note turn-count semantics did not distinguish narrative commits from meta queries/retries.
10. Imported cards/lore/logs lacked an explicit untrusted-instruction firewall.
11. Author-level character naming could leak into diegetic dialogue.
12. Relationship repair could become mechanical coldness signposting.
13. Technical side quests lacked a phase-closure guard.

## Test scope
Only static/logic contract tests are authorized here. No claim of real vector DB, transactional branch backend, visual Inspector, or binary card package runtime testing.
